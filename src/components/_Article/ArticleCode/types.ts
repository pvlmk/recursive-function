import { ReactChildren } from "../../../types/useful";

interface Props {
  children: ReactChildren;
}

interface PropsUI {
  children: ReactChildren;
}

interface Style {}

interface MotionState {}

type OwnProps = {};

type StateProps = {};

type DispatchProps = {};

export {
  Props,
  PropsUI,
  Style,
  MotionState,
  OwnProps,
  StateProps,
  DispatchProps,
};
